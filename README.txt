1.preprocessing.py dùng để xử lý dataset, sau đó lưu dữ liệu đã được xử lý vào file simpsons_trainingset.txt
2.train_model.py dùng để train model word2vec.
    - đọc dữ liệu từ file simpson_trainingset
    - train model
    - lưu các log ra file Logger(1).log
    - biểu diễn trên đồ thị